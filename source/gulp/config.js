module.exports = {
    serve: {
        task:   'serve:proxy',
        host:   'localhost',
        port:   5310,
        url:    'http://adapt.local',
        open:   true,
        tunnel: false,
        log:    'Portal ADAPT'
    }
};
