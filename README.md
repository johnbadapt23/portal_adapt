# Portal ADAPT — WordPress Theme

WordPress theme for the research/membership portal (MemberPress-gated content, partner/sector/topic templates). Built on a Gulp + npm asset pipeline; ships to the staging server via GitHub Actions over SFTP.

## Requirements

- Node **22** (see `.nvmrc` — run `nvm use` if you use nvm)
- npm 10+
- A local WordPress install if you want to run the live-reload dev server (`gulp __start` proxies `http://adapt.local`, configurable in `source/gulp/config.js`)

## Quick start

```bash
npm install          # installs everything in package.json into node_modules
npx gulp _build       # one-off production build → writes into assets/
```

That's it for a one-time build. `assets/css/main.min.css` and `assets/js/main.min.js` get regenerated from `source/scss` and `source/js`; everything else `_build` touches (fonts, icons, images) is also written into `assets/`.

For active local development with live reload:

```bash
npx gulp __start
```

This proxies your local WordPress install (edit `source/gulp/config.js` → `serve.url` if your local domain isn't `adapt.local`) and rebuilds automatically on file changes.

## Build commands

| Command | What it does |
|---|---|
| `npx gulp _build` | Full production build: fonts, icons, images, scripts, styles, then a PHP reload pass |
| `npx gulp __start` | Starts BrowserSync proxy + file watcher for local dev |
| `npx gulp watch` | Just the file watcher (used internally by `__start`) |
| `npx gulp build:styles` | Compile `source/scss` + vendor CSS → `assets/css/{global,core,tpl-agenda,tpl-events}.min.css` (see "CSS bundle split" below) |
| `npx gulp build:scripts` | Bundle `source/js` + vendor JS → `assets/js/main.min.js` |
| `npx gulp build:images` | Optimize `source/images/**` → `assets/images/` |
| `npx gulp build:fonts` | Generate webfonts from `source/fonts/*.{ttf,otf}` (currently no source fonts checked in — this is a no-op unless you add some) |
| `npx gulp build:icons` | Generate the icon font from `source/icons/*.svg` → `assets/fonts/icons.*` + `source/scss/global/_icons.scss` |
| `npx gulp build:favicons` | Regenerate the favicon set via realfavicongenerator.net (needs network access) |
| `npx gulp deploy:zip` / `deploy:ftp` / `deploy:git` | Legacy one-off deploy tasks — not used by CI, kept for manual/local use |

The build pipeline is Gulp 5. Frontend libraries (Slick, Owl Carousel, Magnific Popup, Select2, FlexSlider, MediaElement, isotope-layout, perfect-scrollbar, etc.) are plain npm dependencies — `source/gulp/paths.js` lists exactly which files get pulled from `node_modules` into the bundle.

## Project structure

```
functions.php, header.php, footer.php, index.php, style.css   → active theme entry points
includes/, templates/, memberpress/, download-monitor/         → PHP templates & partials
source/                                                         → build INPUT (scss, js, images, icons, gulp config)
assets/                                                         → build OUTPUT + static production assets (see below)
.github/workflows/deploy.yml                                    → CI build + SFTP deploy
```

A few root PHP files (`nov-functions.php`, `oct-25-header.php`, `old-functions.php`, `one-signal-header.php`, `sept-functions.php`, `june-footer.php`, `header-stage.php`, `nov-header.php`, `oct-25-functions.php`) are dated snapshots left over from past edits — WordPress doesn't load any of them (only `functions.php`/`header.php`/`footer.php` are active). They're kept in git history for reference but excluded from deploy (see the exclude lists in `.github/workflows/deploy.yml`).

### About `assets/`

`assets/` is tracked in git, not ignored. Only `assets/css/{global,core,tpl-agenda,tpl-events}.min.css` and `assets/js/main.min.js` are actually regenerated by the build — the rest of it (most images, all webfonts, `admin.css`, MediaElement's control icons) is static production content with no `source/` equivalent, so it has to be committed directly. CI rebuilds the generated files fresh before every deploy regardless of what's committed.

`assets/css/main.min.css` is a leftover from before the CSS bundle split below - it's no longer regenerated or referenced anywhere and is safe to remove next time someone's touching the deploy pipeline (left in place for now since the delta-sync deploy's `git diff --diff-filter=ACMRT` doesn't pick up deletions, so removing it from git wouldn't actually clean it off the live server without a `sync: full` run - see "Deployment" below).

### CSS bundle split

CSS used to compile into one `main.min.css` combining every template's styles, loaded on every page regardless of which template was active. `source/gulp/tasks/build/styles.js` now compiles four bundles instead, and `header.php` enqueues only the ones a given page needs:

- `global.min.css` — vendor libraries + base/forms/sections/print + header/footer partials. Always loaded.
- `core.min.css` — every `templates/**/*.scss` file except the ones split out below. Default bundle for any template not explicitly special-cased.
- `tpl-agenda.min.css` — `templates/template-agenda.php` only.
- `tpl-events.min.css` — `templates/template-events.php`, loaded alongside `core.min.css` (not instead of it) for `templates/template-events-portal.php`, which shares its wrapper classes but also depends on a shared component partial that lives in `core.min.css`.

Only `template-agenda.php` and `template-events.php` have been verified safe to split out (grepped every other template/component `.php` file for usage of their SCSS selectors, confirmed no cross-references). Most of the site's ~50 page templates share styling through reusable ACF flexible-content component partials (`get_template_part()`) in ways that aren't cleanly attributable to one template without tracing selector-by-selector — those all still load the full `core.min.css`, same CSS they've always gotten, no regression. Splitting more of them out is real, valuable follow-up work, but needs the same verification treatment per template before it's safe.

To split another template: add `source/scss/main-tpl-{name}.scss` (import `global/_mixins`, `global/_variables`, and the template's own `templates/_{name}.scss` — check whether it references any `$variables` beyond those two files first), exclude that `templates/_{name}.scss` from `source/scss/main-core.scss`'s import list, add a `compileBundle(...)` call in `styles.js`, and add the matching `is_page_template()` branch in `header.php`. Add the new filename to `sync-delta-includes` / `ftp-mirror-options` in `deploy.yml` too.

## Deployment (CI/CD)

`.github/workflows/deploy.yml` runs on every push to **`dev`** (or manually via the Actions tab → "Run workflow"):

1. Checks out the repo with full history (`fetch-depth: 0` — required for delta sync), installs Node deps, runs `npx gulp _build`
2. Deploys straight from the repo root over SFTP using [`milanmk/actions-file-deployer`](https://github.com/milanmk/actions-file-deployer), with **delta sync**: it diffs `git diff <previous commit>..<head>` and only uploads/deletes what actually changed, instead of re-uploading the whole theme every run

Delta sync is git-diff based, which has one important consequence: it can't see changes the build step makes to files that aren't re-committed. `assets/css/main.min.css` and `assets/js/main.min.js` are committed files, but CI regenerates them fresh in the runner's working directory without committing the rebuild back — so a commit that only touches `source/scss/*.scss` would show those two files as "unchanged" in git diff even though the freshly-built content on disk is genuinely different. `sync-delta-includes` in the workflow force-includes both of them in every deploy regardless of git diff, specifically to cover this gap.

Dev-only tracked files (`source/`, `gulpfile.js`, `package*.json`, `.nvmrc`, `README.md`, and the confirmed-dead files listed above) are filtered out via `sync-delta-excludes` (git pathspec syntax) for delta runs, and `ftp-mirror-options` (lftp exclude-glob syntax) for full runs — the same list has to exist in both places since the two sync modes use different underlying mechanisms.

Manually running the workflow via the Actions tab lets you choose `sync: full` instead of the default `delta` — useful for the first run after this pipeline changes, or to recover if delta ever drifts from the server's actual state. Full sync mirrors the whole working directory and won't delete anything on the remote that isn't also on the runner.

`main` is not wired to anything — merge `dev` → `main` whenever you want a release checkpoint, it won't trigger a deploy on its own.

### Required GitHub configuration

Set up under **Settings → Environments → Staging** on the repo:

| Name | Type | Value |
|---|---|---|
| `STAGING_USERNAME` | secret | SFTP/SSH username |
| `STAGING_PASS` | secret | **Note:** despite the name, this holds the full SSH private key contents (`-----BEGIN...-----` through `-----END...-----`), not a password. Key auth only. |
| `STAGING_HOST` | variable | Server IP/hostname |
| `STAGING_PORT` | variable | SFTP port (default 22 if unset) |
| `STAGING_REMOTE_PATH` | variable | Absolute path to the theme folder on the server |

The public half of whatever key is in `STAGING_PASS` must already be in `~/.ssh/authorized_keys` for `STAGING_USERNAME` on the server — GitHub Actions can't add it for you.

If the key is passphrase-protected, add one more secret, `STAGING_SSH_PASSPHRASE`, and wire it into the `ssh_passphrase` input in the workflow (not currently set, since no passphrase is configured today).

## Contributing

1. Branch off `dev`
2. `npm install && npx gulp __start` for local dev, or `npx gulp _build` for a one-off build
3. Commit source changes (not the generated `assets/css/main.min.css` / `assets/js/main.min.js` — those are fine to include but will be overwritten by CI regardless)
4. Open a PR into `dev` — merging to `dev` triggers an automatic deploy to staging
