{
    "name": "adapt/portal-adapt",
    "description": "Portal ADAPT WordPress theme - dev-only tooling (PHP_CodeSniffer, WordPress Coding Standards, PHP version compatibility checks). No runtime dependencies; the theme itself doesn't use Composer autoloading.",
    "type": "wordpress-theme",
    "license": "proprietary",
    "private": true,
    "require-dev": {
        "php": ">=8.1",
        "squizlabs/php_codesniffer": "^3.9",
        "wp-coding-standards/wpcs": "^3.1",
        "phpcompatibility/phpcompatibility-wp": "^2.1",
        "dealerdirect/phpcodesniffer-composer-installer": "^1.0"
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    },
    "scripts": {
        "lint:php": "phpcs"
    }
}
