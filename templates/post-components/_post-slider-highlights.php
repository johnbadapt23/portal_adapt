<?php
// Byte-identical to _post-slider.php (differed only in whitespace) - kept
// as a separate file since template-highlights.php references it by this
// name, but delegates to the canonical file to avoid maintaining two
// copies of the same markup.
include locate_template( '/templates/post-components/_post-slider.php' );
