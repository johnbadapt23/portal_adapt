# Dependencies
node_modules/

# Old Bower vendor libraries - superseded by npm packages in node_modules,
# no longer referenced by source/gulp/paths.js. Kept on disk but not tracked.
source/components/

# Build output (regenerated by `npx gulp _build` in CI before every deploy)
assets/

# OS junk
.DS_Store
._*
Thumbs.db

# Logs
npm-debug.log*
*.log

# Env / local config
.env
.env.local

# Editor
.vscode/
.idea/
